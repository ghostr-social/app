[package]
name = "ghostr-hls-manifest"
version = "0.1.0"
edition = "2021"
description = "HLS playlist parsing and URL rewriting."

[dependencies]
anyhow = { workspace = true }
reqwest = { workspace = true }
