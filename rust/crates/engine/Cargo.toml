[package]
name = "ghostr-engine"
version = "0.1.0"
edition = "2021"
description = "Pure deterministic adaptive media allocation, focus, budgets, and host evidence."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
url = { workspace = true }
