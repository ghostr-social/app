[package]
name = "ghostr-engine"
version = "0.1.0"
edition = "2021"
description = "Pure, deterministic delivery policy: catalog, scoring, tiers, focus, budgets, host stats."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
